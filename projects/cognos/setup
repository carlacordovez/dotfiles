#!/usr/bin/env bash
set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

echo 'adding host entries for c1022...'
bash "$DIR/add-host-entries.sh"

echo 'setting up cognos vm...'
bash "$DIR/setup-vm.sh"

